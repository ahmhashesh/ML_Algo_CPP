#include <iostream>
#include <cmath>
#include <numeric>
#include "Math.h"
/**
 * function to calculate the mean
 * **/
//template <typename T> 
//T alg_math::Math_Mean (std::vector<T> Data)
//{
//    T mean = std::accumulate(std::begin(Data), std::end(Data), 0.0) / Data.size();
//    return mean;
//}
//int alg_math::Math_Mean(std::vector<int> Data)
//{
//    int mean = std::accumulate(std::begin(Data), std::end(Data), 0.0) / Data.size();
//    return mean;
//}

